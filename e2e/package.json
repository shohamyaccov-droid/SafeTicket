{
  "name": "safeticket-e2e",
  "private": true,
  "version": "1.0.0",
  "description": "Browser E2E against live SafeTrade (Playwright)",
  "scripts": {
    "test": "playwright test",
    "test:headed": "playwright test --headed"
  },
  "devDependencies": {
    "@playwright/test": "^1.49.1"
  }
}
